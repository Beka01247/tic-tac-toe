# 🎮 Крестики-нолики | Премиум веб-игра

Полностью рабочая веб-игра "Крестики-нолики" с интеграцией Telegram.

## 🛠 Технологии

- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Framer Motion
- Lucide React (иконки)

## 📦 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Telegram бота

#### 2.1. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
cp .env.local.example .env.local
```

Откройте `.env.local` и замените значения на свои:

```env
# ОБЯЗАТЕЛЬНО: Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# ОБЯЗАТЕЛЬНО: Username бота (без @) для deep links
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=tictactoe_notify_bot

# Опционально: Ваш chat_id для fallback (владелец получает все уведомления)
TELEGRAM_CHAT_ID=123456789
```

#### 2.3. Настройка webhook

**Локально (для тестирования с ngrok):**

```bash
# Установите ngrok
brew install ngrok  # на macOS
# или скачайте с https://ngrok.com/

# Запустите туннель
ngrok http 3000

# Скопируйте https URL (например: https://abc123.ngrok.io)
# Установите webhook:
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://abc123.ngrok.io/api/telegram/webhook"}'
```

**На production (Vercel/Netlify):**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.vercel.app/api/telegram/webhook"}'
```

**Проверка webhook:**

```bash
curl "https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo"
```

#### 2.4. Подключение пользователей (self-service)

**Для проверяющих и обычных пользователей:**

1. Откройте сайт
2. Нажмите кнопку "Подключить Telegram"
3. Откроется Telegram с вашим ботом
4. Нажмите "Start"
5. Вернитесь на сайт — статус изменится на "Подключён ✅"
6. Играйте — уведомления будут приходить вам!

**Никакого ручного ввода chat_id!** 🎉

### 3. Запуск локально

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 4. Тестирование Telegram уведомлений

#### В development режиме доступна тестовая панель (правый нижний угол):

1. Нажмите на иконку колбы
2. Кликните "Тест: Победа" или "Тест: Проигрыш"
3. Проверьте, что сообщение пришло в Telegram

**Или протестируйте через игру:**

- Победите компьютер — получите промокод + уведомление в Telegram
- Проиграйте — получите уведомление "Проигрыш" в Telegram
- Ничья — уведомление НЕ отправляется
